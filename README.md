# READ ME
To run, type "make" to compile all Java files. 

Then run the DNS server by typing "java dns.DNSServer" in one terminal, open another terminal to serve as the client. 

Sample queries you can run on the client server: 
```
dig @localhost www.google.com
dig @localhost xkcd.com
```
